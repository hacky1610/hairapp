<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:HairApp.Controls"    
             x:Class="HairApp.Pages.HistoryPage"
             NavigationPage.HasNavigationBar="false"
             BackgroundColor="Transparent">
    <controls:GradientColorStack Style="{StaticResource PageContent}" StartColor="{StaticResource MainColor}" EndColor="{StaticResource NavColor}">
            <ScrollView >
                <StackLayout>
 
                    <StackLayout x:Name="DoneWashDaysContainer">
                        <Label x:Name="mFinishedCareDaysLabel"  Style="{StaticResource Title}"></Label>
                        <StackLayout x:Name="DoneWashDays">
                        </StackLayout>
                    </StackLayout>
                    

                </StackLayout>

            </ScrollView>
    </controls:GradientColorStack>
   
</ContentPage>